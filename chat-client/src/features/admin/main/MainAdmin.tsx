import { Typography } from '@mui/material';
import React from 'react';

function MainAdmin() {
  return (
    <>
      <Typography variant='h4' sx={{ textAlign: 'center' }}>Admin Dashboard</Typography>
    </>
  )
}
export default React.memo(MainAdmin);